# Secrets — never commit these
.env
.env.production
.env.*.local

# Dependencies
node_modules/

# Build output
dist/
build/

# Editor/OS cruft
.DS_Store
*.log

# Scratch / working files (patch diffs delivered during AI-assisted edits)
_patches*/
_to_delete/
PROBE_FILE.txt

# Vite dev-server temp config cache
*.timestamp-*.mjs

# Editor/manual backup files
*.bak

# Large local seed data — not meant to be version-controlled, was slowing
# down every commit/diff. Untracked via `git rm --cached` on 2026-08-14.
backend/src/db/seed-100-saas-ideas.sql
